# debcargo-conf-extra

Placeholder directory (packages/build.sh requires packages/NAME/NAME/ to
exist). The actual work happens in autobuild.sh, which builds additional
rust libraries from the debcargo-conf tree copied next to this package.
